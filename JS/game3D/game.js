// Creating variables
var i=[];
i[0]=1;
i[1]="green";       
console.log(/\d/.test(i[0]))
function update() {
}

function draw() {
    // This is how you draw a rectanglecontext.fillRect(0,0,30,30);
};

function keyup(key) {
    // Show the pressed keycode in the console
};

function mouseup() {
    // Show coordinates of mouse on click
    console.log("Mouse clicked at", mouseX, mouseY);
};
